#include <stdio.h>
int modInverse(int a)
{
 int i;
 for(i = 1; i < 26; i++)
 {
 if((a * i) % 26 == 1)
 return i;
 }
 return -1;
}
int main()
{
 int P[2][2], C[2][2], K[2][2];
 int det, inv, i, j;
 printf("Enter 2x2 plaintext matrix:\n");
 for(i = 0; i < 2; i++)
 for(j = 0; j < 2; j++)
 scanf("%d", &P[i][j]);
 printf("Enter 2x2 ciphertext matrix:\n");
for(i = 0; i < 2; i++)
 for(j = 0; j < 2; j++)
 scanf("%d", &C[i][j]);
 det = (P[0][0] * P[1][1] -
 P[0][1] * P[1][0]) % 26;
 if(det < 0)
 det += 26;
 inv = modInverse(det);
 if(inv == -1)
 {
 printf("Plaintext matrix has no inverse modulo 26.\n");
 return 0;
 }
 K[0][0] = (C[0][0] * P[1][1] - C[0][1] * P[1][0]) * inv;
 K[0][1] = (-C[0][0] * P[0][1] + C[0][1] * P[0][0]) * inv;
 K[1][0] = (C[1][0] * P[1][1] - C[1][1] * P[1][0]) * inv;
 K[1][1] = (-C[1][0] * P[0][1] + C[1][1] * P[0][0]) * inv;
 for(i = 0; i < 2; i++)
 {
 for(j = 0; j < 2; j++)
if(K[i][j] < 0)
 K[i][j] += 26;
 }
 }
 printf("\nRecovered Key Matrix:\n");
 for(i = 0; i < 2; i++)
 {
 for(j = 0; j < 2; j++)
 printf("%d ", K[i][j]);
 printf("\n");
 }
 return 0;
}


 {
 K[i][j] %= 26;
